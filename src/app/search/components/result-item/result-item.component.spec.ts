import { ResultItemComponent } from './result-item.component';

describe('ResultItemComponent', () => {
  const component = new ResultItemComponent();

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
